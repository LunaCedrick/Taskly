/**
 * router.js - View switching only.
 * Scope: Show or hide views based on auth state and navigation.
 * No business logic. No Firestore operations.
 * See: AGENTS.md section 5.
 */

/**
 * Shows the login view and hides the app view.
 * @returns {void}
 */
function showLoginView() {
  // TODO: Session 3.
}

/**
 * Shows the app view and hides the login view.
 * @returns {void}
 */
function showAppView() {
  // TODO: Session 3.
}

/**
 * Shows the named content view inside the app shell.
 * @param {string} viewName - One of dashboard, project, or settings.
 * @returns {void}
 */
function showView(viewName) {
  // TODO: Session 3.
}
